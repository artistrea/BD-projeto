Crie seu venv
`bash
    python3 -m venv .venv
    `

ative-o

```bash
source .venv/bin/activate
```

instale as deps:

```bash
pip install -r requirements.txt
```

RODE:

```bash
python3 -m flask --app src/app run --debug
```
